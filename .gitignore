# Whitelist strategy: ignore everything, then re-include the agent-system files.
# Sessions, sqlite state, logs, auth, and Codex-managed bundles must never be committed.
/*
!/.gitignore
!/AGENTS.md
!/config.toml
!/agents/
!/skills/
/skills/.system/
